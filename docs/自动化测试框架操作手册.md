# API + Web + App 自动化测试框架操作手册

这是一份从安装到落地的完整手册。你同时负责 API、Web、App 自动化时，只需要维护这一套框架、一个 `requirements.txt` 和这一份手册。

---

## 1. 先理解框架怎么分工

三端使用同一种语言和测试入口，但不要把同一个业务场景重复写三遍。

```text
API 用例  -> api/     -> HttpClient -> 后端接口
Web 用例  -> pages/   -> Playwright -> 浏览器
App 用例  -> screens/ -> Appium     -> 真机/模拟器
                         |
                         +-> pytest / fixture / Allure / 配置 / 数据清理共用
```

推荐用例比例：

| 类型 | 建议占比 | 主要覆盖 |
|---|---:|---|
| API | 60%～70% | 参数、权限、边界、异常、业务状态和数据一致性 |
| Web | 15%～20% | 浏览器核心用户链路和前端交互 |
| App | 10%～15% | 移动端核心链路、系统权限和设备交互 |
| 跨系统 E2E | 少量 | 最关键的完整业务闭环 |

原则：业务分支主要交给 API；Web 和 App 少而精，只验证用户真正使用的核心链路。

---

## 2. 软件与版本要求

### 2.1 所有人都要安装

- Git
- Python 3.10、3.11 或 3.12
- VS Code 或 PyCharm
- Allure 命令行（需要查看图形报告时安装）

验证：

```powershell
git --version
python --version
python -m pip --version
```

### 2.2 Web 额外要求

Playwright 浏览器由命令安装，不需要提前安装 Chrome：

```powershell
python -m playwright install chromium
```

需要 Firefox 或 WebKit 时：

```powershell
python -m playwright install firefox webkit
```

### 2.3 Android App 额外要求

- JDK 11 或更高版本
- Android Studio / Android SDK
- Node.js LTS
- Appium Server
- UiAutomator2 Driver
- Appium Inspector
- Android 真机或模拟器

安装 Appium：

```powershell
npm install -g appium
appium driver install uiautomator2
```

验证：

```powershell
java -version
adb version
adb devices
appium --version
appium driver list --installed
```

`adb devices` 必须出现状态为 `device` 的设备。`unauthorized` 表示手机尚未允许 USB 调试。

### 2.4 iOS App 额外要求

iOS 自动化只能在 macOS 上运行，需要 Xcode、可用的模拟器或真机，并安装 XCUITest Driver：

```bash
appium driver install xcuitest
```

---

## 3. 第一次安装项目

### 3.1 获取代码并创建虚拟环境

```powershell
git clone <你的仓库地址>
cd web-api-app-autotest
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

激活成功后，命令行前面会出现 `(.venv)`。每次重新打开终端都要重新激活。

如果 PowerShell 不允许激活脚本：

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
.\.venv\Scripts\Activate.ps1
```

### 3.2 一次安装全部依赖

项目只有一个依赖文件：

```powershell
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
python -m playwright install chromium
```

`requirements.txt` 已包含 API、Web、App、MySQL、Redis、Allure、pytest 和 Ruff。

### 3.3 创建本地敏感配置

```powershell
Copy-Item .env.example .env
```

`.env` 已被 `.gitignore` 排除，不要强制提交。

### 3.4 第一次必须执行的检查

```powershell
python scripts/automation.py doctor --env uat
python scripts/automation.py self-test
```

- `doctor`：检查 Python、依赖、配置、Playwright、Appium、ADB、Java。
- `self-test`：运行完全离线的框架自测，不访问业务系统。

---

## 4. 目录说明

```text
api/                 API 业务封装
pages/               Web Page Object
screens/             App Screen Object
testcases/api/       真实 API 用例
testcases/web/       真实 Web 用例
testcases/app/       真实 App 用例
tests/framework/     框架自身离线测试
fixtures/            登录态、数据准备、自动清理
clients/             MySQL、Redis、通知、邮件
core/                HTTP、断言、Web/App driver、安全保护
config/              多环境配置
data/                YAML、JSON、Excel、文本测试数据
utils/               日志、随机数据、日期、JSONPath 等工具
scripts/             统一命令和 CI 汇总脚本
reports/             运行生成的报告，不提交 Git
logs/                运行日志，不提交 Git
```

最重要的放置规则：

- 接口路径和请求细节放 `api/`。
- Web 定位器和页面行为放 `pages/`。
- App 定位器和页面行为放 `screens/`。
- `testcases/` 只写场景和断言。
- 多组数据放 `data/`。
- 通用前置和清理放 `fixtures/`。
- 账号、密码、token、webhook 只放 `.env` 或 CI Secret。

---

## 5. 配置环境

### 5.1 非敏感配置：`config/config.yaml`

按公司实际环境修改：

```yaml
common:
  timeout: 30
  retry: 0
  log_level: INFO
  log_response: true
  log_response_max: 1000

sit:
  api_base_url: "https://sit-api.company.com"
  web_base_url: "https://sit.company.com"
  db:
    host: "sit-db.company.com"
    port: 3306
    name: "app_db"
  redis:
    host: "sit-redis.company.com"
    port: 6379
    db: 0

uat:
  api_base_url: "https://uat-api.company.com"
  web_base_url: "https://uat.company.com"
  db:
    host: "uat-db.company.com"
    port: 3306
    name: "app_db"
  redis:
    host: "uat-redis.company.com"
    port: 6379
    db: 0
```

### 5.2 敏感配置：`.env`

```dotenv
ENV=uat
TEST_USERNAME=测试账号
TEST_PASSWORD=测试密码
API_TOKEN=

DB_USER=数据库账号
DB_PASSWORD=数据库密码
REDIS_PASSWORD=

DINGTALK_WEBHOOK=
DINGTALK_SECRET=
WECOM_WEBHOOK=

SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
EMAIL_TO=

NOTIFY_ON_FINISH=
REPORT_URL=
```

修改后检查：

```powershell
python scripts/automation.py doctor --env uat
```

---

## 6. 统一运行命令

以后优先使用 `scripts/automation.py`，不需要记多套入口。

```powershell
# 环境检查和框架自测
python scripts/automation.py doctor --env uat
python scripts/automation.py self-test

# 按端执行
python scripts/automation.py test --type api --env uat
python scripts/automation.py test --type web --env uat
python scripts/automation.py test --type app --env uat

# 三端一起执行
python scripts/automation.py test --type all --env uat

# 按范围执行
python scripts/automation.py test --type all --env uat --marker smoke
python scripts/automation.py test --type api --env uat --marker regression

# Web 调试
python scripts/automation.py test --type web --env uat --headed
python scripts/automation.py test --type web --env uat --headed --slowmo 500
python scripts/automation.py test --type web --env uat --browser firefox

# 清理报告、日志和缓存
python scripts/automation.py clean
```

只运行一个文件或用例：

```powershell
python -m pytest testcases/api/test_order_api.py --env uat
python -m pytest testcases/web/test_login_web.py::TestLoginWeb::test_login_success --env uat
```

业务目录没有收集到用例时默认失败，防止 CI 假绿色。仅模板初始化阶段可临时使用：

```powershell
python scripts/automation.py test --type api --env uat --allow-empty
```

正式 CI 不要使用 `--allow-empty`。

---

## 7. Marker 规则

| Marker | 用途 |
|---|---|
| `api` / `web` / `app` | 测试端类型 |
| `smoke` | 每次提交执行的核心链路 |
| `regression` | 定时完整回归 |
| `p0` / `p1` / `p2` | 业务优先级 |
| `prod_safe` | 人工审核后的生产只读用例 |
| `flaky` | 已登记的不稳定用例，不代表自动重试 |
| `timeout(秒)` | 覆盖单条用例的默认超时时间 |

每个真实用例至少标记端类型和优先级：

```python
@pytest.mark.api
@pytest.mark.smoke
@pytest.mark.p0
def test_create_order():
    ...
```

框架使用 `pytest-timeout` 防止请求、浏览器或设备异常后无限卡住：

| 用例类型 | 默认超时 |
|---|---:|
| API | 60 秒 |
| Web | 180 秒 |
| App | 300 秒 |
| 没有端类型标记 | 300 秒兜底 |

超时按“单条测试用例”计算，不是整批任务的总时长。确实需要更长时间时，只覆盖该用例：

```python
@pytest.mark.app
@pytest.mark.timeout(600)
def test_large_file_upgrade(app_driver):
    ...
```

不要为了绕过死锁而随意增大超时；先检查网络请求、显式等待、Appium Server 和设备状态。

---

## 8. API 自动化操作步骤

### 8.1 第一步：封装业务 API

假设接口是 `GET /api/products/{id}`，创建 `api/product_api.py`：

```python
"""商品模块 API 封装。"""

import allure

from api.base_api import BaseApi


class ProductApi(BaseApi):
    @allure.step("查询商品: id={product_id}")
    def get_product(self, product_id: int):
        return self.client.get(f"/api/products/{product_id}")

    @allure.step("创建商品")
    def create_product(self, payload: dict):
        return self.client.post("/api/products", json=payload)
```

不要在业务 API 类里写完整域名，环境地址由配置统一管理。

### 8.2 第二步：编写用例

创建 `testcases/api/test_product_api.py`：

```python
"""商品接口测试。"""

import allure
import pytest

from api.product_api import ProductApi
from core.assertions import Assert


@allure.feature("商品接口")
@pytest.mark.api
class TestProductApi:
    @pytest.mark.smoke
    @pytest.mark.p0
    def test_get_product_success(self, api_client):
        response = ProductApi(client=api_client).get_product(product_id=1001)

        Assert.status_code(response, 200)
        Assert.json_value(response, "code", 0)
        Assert.jsonpath(response, "$.data.id", 1001)
```

不要只断言 HTTP 200；还要断言业务 code、关键字段、状态和数据类型。

### 8.3 登录态复用

多个 API 模块共享同一个 `api_client`：

```python
def test_order_flow(api_client, env_settings):
    user_api = UserApi(client=api_client)
    order_api = OrderApi(client=api_client)

    user_api.login(env_settings.username, env_settings.password)
    response = order_api.create_order(product_id=1001, quantity=1)
    Assert.status_code(response, 200)
```

登录后 token/cookie 保存在同一个 Session 中。

### 8.4 数据驱动

`data/login_data.yaml`：

```yaml
- case_id: login_success
  username: valid_user
  password: correct_password
  expected_status: 200
- case_id: login_wrong_password
  username: valid_user
  password: wrong_password
  expected_status: 401
```

用例：

```python
login_cases = load_yaml("login_data.yaml")


@pytest.mark.parametrize("case", login_cases, ids=[case["case_id"] for case in login_cases])
def test_login(case, api_client):
    response = UserApi(client=api_client).login(case["username"], case["password"])
    Assert.status_code(response, case["expected_status"])
```

### 8.5 常用断言

```python
Assert.status_code(response, 200)
Assert.json_value(response, "code", 0)
Assert.jsonpath(response, "$.data.status", "PAID")
Assert.jsonpath_exists(response, "$.data.token")
Assert.contains(response.json(), "data")
Assert.equal(actual, expected)
Assert.not_empty(value)
Assert.approx(actual_amount, expected_amount, tol=0.01)
Assert.match_schema(response, schema)
```

### 8.6 数据库断言和自动清理

```python
def test_create_order(api_client, db, clean_data):
    response = OrderApi(client=api_client).create_order(product_id=1001, quantity=1)
    order_id = response.json()["data"]["order_id"]

    # 创建成功后立即登记清理，后续断言失败也会执行
    clean_data.add_table("orders", "order_id=%s", [order_id])

    row = db.query_one(
        "SELECT status, amount FROM orders WHERE order_id=%s",
        [order_id],
    )
    Assert.equal(row["status"], "CREATED")
```

SQL 必须参数化，不要拼接用户输入。生产环境 DB/Redis 写操作会被框架直接阻止。

### 8.7 API 用例设计清单

每个重要接口至少考虑：

- 正常参数。
- 必填参数缺失。
- 空值、0、负数、最大长度、超长值。
- 不存在的业务 ID。
- 无 token、过期 token、错误角色、越权访问。
- HTTP 状态码和业务 code。
- 关键响应字段和 JSON Schema。
- 写操作后的查询接口或数据库验证。
- 重复请求和幂等性。

---

## 9. Web 自动化操作步骤

### 9.1 配置浏览器

`config/config.yaml`：

```yaml
web:
  browser: chromium
  headless: true
  slow_mo: 0
  viewport:
    width: 1920
    height: 1080
  trace: true
```

本地调试使用 `--headed --slowmo 500`，CI 使用无头模式。

### 9.2 获取稳定定位器

优先级：

```text
data-testid / id / role > 稳定文本 > CSS class > XPath
```

建议推动开发为关键控件提供 `data-testid`。不要依赖动态 class、元素下标和超长 XPath。

Playwright 录制：

```powershell
python -m playwright codegen --target python https://你的系统地址
```

录制结果只用于获取操作步骤和定位器，最终要整理为 Page Object，不要把裸录制代码直接堆进用例。

### 9.3 创建 Page Object

`pages/login_page.py`：

```python
"""登录页面对象。"""

import allure

from pages.base_page import BasePage


class LoginPage(BasePage):
    URL = "/login"
    INPUT_USERNAME = "[data-testid='username']"
    INPUT_PASSWORD = "[data-testid='password']"
    BTN_LOGIN = "[data-testid='login-btn']"
    USER_AVATAR = "[data-testid='user-avatar']"

    @allure.step("执行登录: {username}")
    def login(self, username: str, password: str):
        self.open(self.URL)
        self.fill(self.INPUT_USERNAME, username)
        self.fill(self.INPUT_PASSWORD, password, sensitive=True)
        self.click(self.BTN_LOGIN)
```

密码、token、手机号等敏感输入使用 `sensitive=True`。框架也会根据定位器名称自动识别常见敏感字段。

### 9.4 创建 Web 用例

```python
"""Web 登录测试。"""

import allure
import pytest

from pages.login_page import LoginPage


@allure.feature("Web 登录")
@pytest.mark.web
class TestLoginWeb:
    @pytest.mark.smoke
    @pytest.mark.p0
    def test_login_success(self, page, env_settings):
        login_page = LoginPage(page)
        login_page.login(env_settings.username, env_settings.password)
        login_page.expect_visible(LoginPage.USER_AVATAR)
```

Playwright 自带等待。不要写固定 `sleep`，使用 `expect_*`、Locator 或明确的事件等待。

### 9.5 Web 用例控制数量

适合 Web 自动化：

- 登录和退出。
- 核心下单、支付前置、审批、发布等主链路。
- 关键页面展示和表单交互。
- 浏览器兼容性验证。

不适合大量放在 Web：

- 参数组合和边界值。
- 大量权限矩阵。
- 后端复杂状态流转。
- 可以通过 API 快速覆盖的业务分支。

---

## 10. App 自动化操作步骤

### 10.1 启动设备和 Appium

```powershell
adb devices
appium
```

保持 Appium 终端运行，再打开另一个终端执行测试。

### 10.2 获取包名和 Activity

启动目标 App 后：

```powershell
adb shell dumpsys window | findstr mCurrentFocus
```

输出中 `/` 前面是 `appPackage`，后面是 `appActivity`。

### 10.3 配置 capabilities

`config/config.yaml`：

```yaml
app:
  appium_server: "http://127.0.0.1:4723"
  platform: "Android"
  android:
    platformName: "Android"
    automationName: "UiAutomator2"
    deviceName: "emulator-5554"
    appPackage: "com.company.app"
    appActivity: ".MainActivity"
    noReset: true
    autoGrantPermissions: true
```

### 10.4 使用 Appium Inspector 获取定位器

1. 启动设备和 Appium Server。
2. 打开 Appium Inspector。
3. 填入与 `config.yaml` 一致的 capabilities。
4. 创建 Session。
5. 点击控件，读取 `resource-id` 或 `content-desc`。

定位优先级：

```text
resource-id > accessibility id > Android UiAutomator > XPath
```

### 10.5 创建 Screen Object

```python
"""App 登录页面对象。"""

import allure

from screens.base_screen import BaseScreen


class LoginScreen(BaseScreen):
    INPUT_USERNAME = (BaseScreen.ID, "com.company.app:id/username")
    INPUT_PASSWORD = (BaseScreen.ID, "com.company.app:id/password")
    BTN_LOGIN = (BaseScreen.ID, "com.company.app:id/login")
    HOME_TAB = (BaseScreen.ACCESSIBILITY_ID, "home_tab")

    @allure.step("App 登录: {username}")
    def login(self, username: str, password: str):
        self.input(*self.INPUT_USERNAME, username)
        self.input(*self.INPUT_PASSWORD, password, sensitive=True)
        self.click(*self.BTN_LOGIN)

    def is_login_success(self) -> bool:
        return self.is_displayed(*self.HOME_TAB)
```

### 10.6 创建 App 用例

```python
"""App 登录测试。"""

import allure
import pytest

from screens.login_screen import LoginScreen


@allure.feature("App 登录")
@pytest.mark.app
class TestLoginApp:
    @pytest.mark.smoke
    @pytest.mark.p0
    def test_login_success(self, app_driver, env_settings):
        screen = LoginScreen(app_driver)
        screen.login(env_settings.username, env_settings.password)
        assert screen.is_login_success(), "登录后未进入首页"
```

教学示例默认有 `skip`。真实设备、包名、Activity 和定位器全部准备好后再删除 `skip`。

### 10.7 App 稳定性规则

- 不使用固定 `sleep`，使用显式等待。
- 优先稳定 ID，不使用坐标点击作为主要方案。
- 每条用例明确初始状态。
- 测试账号和测试数据相互隔离。
- 失败时查看截图、package、activity 和 Appium Server 日志。
- App 测试成本高，先覆盖 3～5 条核心冒烟，再逐步增加。

---

## 11. 三端协作的推荐写法

同一个“下单”功能：

- API：覆盖正常、库存不足、优惠、权限、重复提交、状态流转。
- Web：只验证浏览器可以完成一次正常下单。
- App：只验证移动端可以完成一次正常下单以及必要的系统交互。

UI/App 的数据准备优先调用 API：

```text
API 创建测试用户和商品 -> Web/App 执行核心操作 -> API/DB 验证结果 -> API/DB 清理
```

但如果测试目标本身就是“页面创建订单”，不能用 API 替代这个被测步骤。

Web 流程背后的 API 序列可以通过 `network_recorder` fixture 捕获：

```python
def test_capture_flow(page, network_recorder):
    LoginPage(page).login("user", "password")
    # 继续执行核心 UI 流程
    network_recorder.print_summary()
    network_recorder.save("captured_apis.json")
```

录制内容会经过脱敏，但仍应把输出当成测试内部资料，不提交真实业务数据。

---

## 12. 常用 Fixture 和工具

### 12.1 Fixture

| Fixture | 用途 |
|---|---|
| `env_settings` | 获取当前环境、账号和配置 |
| `api_client` | 用例级干净 HTTP Session |
| `logged_in_client` | Session 级登录态复用 |
| `page` | Playwright 页面 |
| `app_driver` | Appium driver，用例后自动退出 |
| `db` | MySQL 查询客户端 |
| `clean_data` | 注册 SQL 或回调，结束后逆序清理 |
| `network_recorder` | 捕获 Web 流程中的 API 调用 |

### 12.2 数据工具

```python
from utils.data_loader import load_excel, load_json, load_yaml, read_lines
from utils.extractor import extract, extract_all
from utils.random_data import random_email, random_name, unique_id
from utils.retry import wait_until
```

异步落库或消息消费使用轮询：

```python
order = wait_until(
    lambda: db.query_one("SELECT * FROM orders WHERE id=%s", [order_id]),
    timeout=20,
    interval=1,
    desc="等待订单落库",
)
```

不要用全局失败重试掩盖真实缺陷。只对明确幂等、确认属于基础设施波动的操作使用重试。

---

## 13. 报告、日志和失败排查

默认结果目录：

```text
reports/allure-results
```

查看报告：

```powershell
allure serve reports/allure-results
```

失败时框架会尽量收集：

- Web/App 截图。
- Web 当前 URL 和标题。
- App 当前 package 和 activity。
- API 请求、响应、状态码和耗时。
- Playwright Trace（配置开启时）。

日志保存在 `logs/`，敏感输入、token、密码、手机号、SQL 参数和 Redis 值不会原样输出。

并行 pytest 进程不能同时对同一个 Allure 目录使用 `--clean-alluredir`。CI 必须使用独立目录：

```text
reports/allure-framework
reports/allure-api
reports/allure-web
reports/allure-app
```

---

## 14. 生产环境安全规则

普通生产命令会直接终止：

```powershell
python scripts/automation.py test --type api --env prod
```

确实需要生产只读验证时，必须同时满足：

1. 用例经过人工审核并标记 `prod_safe`。
2. 命令带 `--allow-prod`。
3. 环境变量严格等于 `ALLOW_PROD_TESTS=1`。

```powershell
$env:ALLOW_PROD_TESTS="1"
python scripts/automation.py test --type api --env prod --allow-prod
Remove-Item Env:ALLOW_PROD_TESTS
```

即使授权成功：

- 只收集 `prod_safe` 用例。
- HTTP 只允许 GET、HEAD、OPTIONS。
- HTTP 只允许访问配置中的同源 API。
- DB 和 Redis 写操作会被阻止。
- 不允许使用创建数据和自动清理 fixture。

生产数据库账号仍应由 DBA 设置为只读，不要只依赖代码保护。

---

## 15. 并行执行和数据隔离

框架支持 pytest-xdist：

```powershell
python -m pytest testcases/api -n 4 --env uat
```

并行前必须满足：

- 每个用例使用独立测试数据。
- 测试数据包含唯一 `run_id` 或 `worker_id`。
- 不同进程不要共用同一个可修改账号。
- 清理逻辑只能删除本次创建的数据。
- App 并行需要一台设备对应一个 worker，不能多个进程抢同一台设备。

推荐唯一数据：

```python
order_no = unique_id("AUTO_ORDER")
```

---

## 16. CI 接入

仓库提供：

- `.github/workflows/automation-test.yml`
- `.gitlab-ci.yml`
- `Jenkinsfile`

统一流程：

```text
安装 requirements.txt
  -> Ruff
  -> 框架 self-test
  -> API/Web 冒烟或回归
  -> 合并 Allure
  -> 钉钉/企微/邮件通知
```

### 16.1 CI Secret

根据实际使用配置：

```text
TEST_USERNAME / TEST_PASSWORD / API_TOKEN
DB_USER / DB_PASSWORD / REDIS_PASSWORD
DINGTALK_WEBHOOK / DINGTALK_SECRET
WECOM_WEBHOOK
SMTP_HOST / SMTP_PORT / SMTP_USER / SMTP_PASSWORD / EMAIL_TO
REPORT_URL
```

不要把真实值写进仓库。

### 16.2 GitHub Actions

1. 在仓库 Settings -> Secrets and variables -> Actions 配置 Secret。
2. 主分支不是 `main` 时修改 workflow 触发分支。
3. 需要报告页面时启用 GitHub Pages。
4. Runner 无法访问内网测试环境时使用 self-hosted runner。

### 16.3 GitLab CI

1. Settings -> CI/CD -> Variables 配置 masked 变量。
2. 创建 Pipeline Schedule 执行每日 regression。
3. 内网环境使用公司 Runner。
4. GitLab Pages 不可用时把报告上传到内网服务器。

### 16.4 Jenkins

1. 安装 JUnit 和 Allure 插件。
2. 配置 Jenkinsfile 使用的凭据 ID。
3. Agent 安装 Python、Playwright 浏览器。
4. 每次提交运行 smoke，定时任务运行 regression。

### 16.5 App CI 特别说明

当前通用 CI 默认运行 API 和 Web。App CI 需要公司提供以下其中一种能力后再接入：

- 自托管 Runner + 固定真机。
- 自托管 Runner + Android 模拟器。
- 云真机平台。

App CI 还需要安装包来源、设备锁、Appium Server 生命周期和独立 Allure 目录。没有设备基础设施时，不要配置一个表面存在但无法稳定执行的 App job。

---

## 17. 去新公司后的落地顺序

### 17.1 先申请这些信息和权限

| 类别 | 需要内容 |
|---|---|
| 代码 | 仓库读写权限、分支规则、CI 平台 |
| 环境 | SIT/UAT 地址、VPN、网络白名单 |
| API | Swagger/OpenAPI、鉴权方式、测试账号和角色 |
| 数据 | 测试库只读账号、造数方式、清理方式 |
| Web | 稳定 `data-testid`、浏览器范围 |
| App | APK/IPA、包名、签名、真机/模拟器、构建渠道 |
| 排障 | 日志平台、trace ID、服务负责人 |
| 通知 | 钉钉/企微机器人或 SMTP |

### 17.2 第一周：最小闭环

先完成：

- 1 条 API 用例。
- 1 条 Web 用例。
- 1 条 App 用例。
- 能运行、能出报告、失败能定位。

不要一开始搬运几十条用例。

### 17.3 第二周：API 和测试数据

- 登录态和多角色账号。
- 数据工厂和唯一编号。
- 自动清理。
- 10～20 条核心 API 冒烟。
- 查询接口或数据库二次断言。

### 17.4 第三周：Web 核心链路

- 5～10 条 Web 冒烟。
- 推动开发补 `data-testid`。
- 使用 API 准备数据。
- 接入提交或合并请求流水线。

### 17.5 第四周：App 和定时回归

- 先支持用户占比最高的平台。
- 3～5 条 App 核心冒烟。
- 准备固定设备或云真机。
- 每日定时运行 regression。
- 报告和失败通知进入团队群。

---

## 18. 编码规范

### 18.1 命名

| 对象 | 规则 | 示例 |
|---|---|---|
| 文件 | 小写下划线 | `order_api.py` |
| 类 | 大驼峰 | `OrderApi` |
| 方法 | 小写下划线 | `create_order` |
| 定位器 | 大写下划线 | `BTN_SUBMIT` |
| 测试文件 | `test_<模块>_<端>.py` | `test_order_api.py` |
| 测试方法 | `test_<场景>` | `test_create_order_success` |

### 18.2 注释

- 每个业务模块顶部写中文 docstring，说明职责。
- 业务方法名称表达业务语义。
- 注释解释“为什么”，不要逐行复述代码。
- 复杂数据结构和安全规则必须说明。
- 简单 getter、click 等不需要重复注释。

### 18.3 提交前

```powershell
python -m ruff format .
python -m ruff check .
python scripts/automation.py self-test
```

---

## 19. 常见问题

### 19.1 `ModuleNotFoundError`

```powershell
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
```

### 19.2 Playwright 提示浏览器不存在

```powershell
python -m playwright install chromium
```

### 19.3 没有收集到业务用例

检查：

- 文件是否放在 `testcases/api|web|app`。
- 文件名是否以 `test_` 开头。
- 测试函数是否以 `test_` 开头。
- 是否添加了正确的 `api/web/app` marker。
- `--marker` 表达式是否把所有用例过滤掉。

模板初始化阶段才可使用 `--allow-empty`。

### 19.4 API 401/403

- 检查账号、token、角色。
- 检查是否使用同一个 `api_client` 保持登录态。
- 查看 Allure 中经过脱敏的请求头和响应。

### 19.5 Web 找不到元素

- 用 Playwright Inspector/codegen 重新确认定位器。
- 优先改为 `data-testid`、id 或 role。
- 等待业务状态，不使用固定 sleep。
- 查看失败截图和 Trace。

### 19.6 App 无法创建 Session

依次检查：

```powershell
adb devices
appium driver list --installed
python scripts/automation.py doctor --env uat
```

然后确认 Appium Server、deviceName、appPackage、appActivity 和安装包状态。

### 19.7 Allure 清理报错或结果丢失

不要让两个 pytest 进程同时使用同一个结果目录并开启 `--clean-alluredir`。为每个并行任务指定独立目录。

### 19.8 用例偶发失败

先判断原因：

- 定位器不稳定。
- 测试数据冲突。
- 服务异步未完成。
- 环境网络波动。
- 用例之间有顺序依赖。

修复根因，不要直接增加全局重试。

---

## 20. 日常操作清单

### 每天开始工作

```powershell
git pull
.\.venv\Scripts\Activate.ps1
python scripts/automation.py doctor --env uat
```

### 编写新功能自动化

1. 确认业务场景和接口文档。
2. 先决定主要放 API、Web 还是 App，避免三端重复。
3. 创建业务 API/Page/Screen 对象。
4. 在 `testcases/` 编写场景和断言。
5. 创建后立即登记数据清理。
6. 本地运行单文件。
7. 运行 smoke。
8. 查看 Allure 和日志。
9. Ruff + self-test。
10. 提交代码并观察 CI。

### 发布前回归

```powershell
python scripts/automation.py self-test
python scripts/automation.py test --type api --env uat --marker regression
python scripts/automation.py test --type web --env uat --marker regression
python scripts/automation.py test --type app --env uat --marker regression
```

### 最终验收标准

- 环境配置不包含真实密码。
- `doctor` 没有阻断项。
- 框架 self-test 通过。
- smoke 用例数量不为 0。
- API 断言不仅检查 HTTP 200。
- Web/App 没有固定 sleep 和脆弱 XPath。
- 写数据用例有清理策略。
- 失败报告能看到足够证据。
- CI 使用独立 Allure 目录。
- 生产环境不默认开放。

完成以上项目后，这套框架就可以作为你同时维护 API、Web、App 自动化的统一工作台。
